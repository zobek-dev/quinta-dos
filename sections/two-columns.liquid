<div class="wrapper my-8 py-4 md:py-8">
  <div class="flex flex-col-reverse md:flex-row md:items-center gap-8">
    <div class="md:w-1/2">
      <h2 class="uppercase font-black leading-none title-rich mb-4 text-[25px] lg:text-[30px] text-center lg:text-left">{{ section.settings.title }}</h2>
      <div class="prose mb-4">
        {{ section.settings.content }}
      </div>
      <a href="{{ section.settings.btn_link }}" class="btn-rounded btn-primary">
        {{ section.settings.btn_text }}
      </a>
    </div>
    <div class="md:w-1/2">
      {% if section.settings.image != blank %}
      {{ section.settings.image | image_url: width: 800 | image_tag: loading: 'lazy', class: 'aspect-[16/9] object-center object-cover' }}
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
  {
    "name": "Two columns",
    "tag": "section",
    "settings": [
      {
        "type": "richtext",
        "id": "title",
        "label": "Título de la sección",
        "default": "<p>Hola, somos <strong>Quinta desarmaduría</strong></p>"
      },
      {
        "type": "richtext",
        "id": "content",
        "label": "Contenido de la sección",
        "default": "<p>Nos ubicamos en la Quinta Región y abastecemos a todo Chile con sus necesidades de accesorios y repuestos de vehículos, atendidos por nuestro equipo de venta y mecánicos especialistas. Trabajamos todas las marcas del mercado y contamos con stock de renovación constante de vehículos en desarme.</p>"
      },
      {
        "type": "text",
        "id": "btn_text",
        "label": "Texto del boton",
        "default": "Cónoce al equipo"
      },
      {
        "type": "url",
        "id": "btn_link",
        "label": "Link del botón"
      },
      {
        "type": "image_picker",
        "id": "image",
        "label": "Imagen de la sección"
      }
    ],
    "presets": [
      {
        "name": "Two Columns"
      }
    ]
  }
{% endschema %}  